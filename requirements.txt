# Core dependencies
pydantic>=2.0.0
python-dotenv>=1.0.0
httpx>=0.27.0
cryptography>=42.0.0

# Testing
pytest>=7.4.0

# Terminal UI (optional but nice for output)
rich>=13.0.0

# Token counting (optional - falls back to word-based approximation)
# Uncomment to use more accurate token counting:
# tiktoken>=0.5.0

# Fetch.ai uAgents framework + Chat Protocol (Agentverse / ASI:One)
# 0.25.2 pulls uagents-core>=0.4.6, which provides uagents_core.contrib.protocols.chat (spec v0.3.0)
uagents>=0.25.2

# HTTP client for the ESPN anchor layer (Phase 2)
httpx>=0.27.0

# Claude API (optional for advanced compression)
anthropic>=0.18.0

# Phase 3 Browserbase noisy layer (OPTIONAL — install with: pip install -e ".[scrape]")
# The noisy collector degrades to disk cache / fixtures when these or
# BROWSERBASE_API_KEY are absent, so the demo runs offline.
# playwright>=1.44.0
# browserbase>=1.0.0

# Note: For the MVP standalone app, uagents is not required.
# For Agentverse deployment, install with: pip install -r requirements.txt
# For Claude-based claim extraction, set ANTHROPIC_API_KEY environment variable
