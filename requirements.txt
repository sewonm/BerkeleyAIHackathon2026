# Core dependencies
pydantic>=2.0.0
python-dotenv>=1.0.0
httpx>=0.27.0
cryptography>=42.0.0

# Testing
pytest>=7.4.0

# Terminal UI (optional but nice for output)
rich>=13.0.0

# Token counting (optional - falls back to word-based approximation)
# Uncomment to use more accurate token counting:
# tiktoken>=0.5.0

# Fetch.ai uAgents (for Agentverse deployment)
uagents>=0.12.0

# Claude API (optional for advanced compression)
anthropic>=0.18.0

# Redis — shared agent memory and vector search
redis>=5.0.5
redisvl>=0.3.0

# Semantic embeddings for Redis HNSW vector search in ConsensusClusterer
sentence-transformers>=2.7.0

# Redis Agent Memory Server — persistent session + long-term memory
redis-agent-memory>=0.1.0

# Redis Iris LangCache — semantic LLM response caching
langcache>=0.1.0

# Note: For the MVP standalone app, uagents is not required.
# For Agentverse deployment, install with: pip install -r requirements.txt
# For Claude-based claim extraction, set ANTHROPIC_API_KEY environment variable
